{
    "training_data_path": "./data/training_fixed",
    "checkpoint_path": "./checkpoints",

    "backbone": "resnet50",
    "criterion": "RMSE",
    "optimizer": "ADAM",

    "device": "cuda",
    "num_classes": 30,
    "batch_size": 32,
    "epochs": 10,
    "train_split": 0.7,
    "nfolds": 3,

    "hyperparameters": {
        "optimizer": {
            "lr": 3e-4,
            "weight_decay": 5e-4
        },
        "mean": 0,
        "std": 1,
        "seed": 42
    },

    "verbose": 10
}