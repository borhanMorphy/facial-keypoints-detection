{
    "training_data_path": "./data/training_fixed",
    "checkpoint_path": "./checkpoints",

    "backbone": "shufflenet",
    "criterion": "RMSE",
    "optimizer": "ADAM",

    "device": "cuda",
    "precision":"float32",
    "num_classes": 30,
    "batch_size": 32,
    "epochs": 10,
    "train_split": 0.7,
    "nfolds": 3,

    "hyperparameters": {
        "optimizer": {
            "lr": 3e-4,
            "weight_decay": 5e-4
        },
        "mean": 0,
        "std": 1
    },

    "verbose": 10
}